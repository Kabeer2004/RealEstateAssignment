DATABASE_URL="postgresql+asyncpg://user:password@localhost/capmatch"
REDIS_HOST="localhost"
REDIS_PASSWORD="" # Leave blank if no password, otherwise fill in your redis password
BLS_API_KEY="" # Get from https://data.bls.gov/registrationEngine/
CENSUS_API_KEY="" # Optional, get from https://api.census.gov/data/key_signup.html

CORS_ORIGINS="http://localhost:3000" # Comma-separated list of allowed origins
